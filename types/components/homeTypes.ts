import { IconDefinition } from '@fortawesome/fontawesome-common-types';

export type SkillsHomeItemType = {
  title: string;
  highlight?: boolean;
  icon: IconDefinition;
};

export type CooperationHomeItemType = {
  number: number;
};
