import cx from 'classnames';

import style from './AboutHomeSection.module.scss';

export const AboutHomeSection = () => {
  return (
    <section className={style.main}>
      <div className={cx('layout_wrapper', style.wrapper)}>AboutHomeSection</div>
    </section>
  );
};
